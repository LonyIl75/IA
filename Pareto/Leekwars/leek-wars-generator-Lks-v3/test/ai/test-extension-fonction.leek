// useChip(CHIP_FORTRESS, getLeek());

print_hello_test2("Albert"); //JuStatFunctions : import / extension fonctionnelle

print_hello_test("Bertrand"); //system_function : fonction systeme supplementaire


function danserSurLaPlage() {
	debug("Danser Sur la Plage !");
}

//danserSurLaPlage(); // fait proquer LeekExpressionFunction NAME // la simple declaration ne la fait pas proquer car pas encore utiliser 

var enemy = getNearestEnemy();

if (count(getWeapons())) {
	setWeapon(getWeapons()[0]);
}

moveToward(enemy);

useWeapon(enemy);