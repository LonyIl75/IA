package todorov.tp1;

import java.util.HashMap;
import java.util.Set;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class ObjectJena {
	/*static  String objetURI ; //main object URI 
	HashMap<String , String > proprieties ;
	Set<String> labels ; 
	static final String delimURI =":";
	static final String coRefPattern ="http:\\/\\/.+\\/\\"+delimURI+"[a-zA-Z0-9_]+" ;
	int nbCoRef = 0 ; 
	
	public ObjectJena  (String _URI)
	{
		objetURI = _URI ;
		proprieties = null;
		labels = null ;
	}
	public boolean isCoRef(Pair<String,String> key_value) {
		Pattern pattern1 = Pattern.compile(coRefPattern );
		Matcher matcher1 = pattern1.matcher(key_value.getValue()) ;
		return matcher1.find();
	}
	public void remove(String key ) {
		if(isCoRef(new Pair<String ,String >(key,proprieties.get(key))))nbCoRef--;
		proprieties.remove(key);
		labels.remove(key);
	}
*/

}
